{
  "name": "@agent-artifacts/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc -b --pretty false",
    "lint": "tsc -b --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-artifacts/artifact": "workspace:*",
    "@agent-artifacts/auth": "workspace:*",
    "@agent-artifacts/config": "workspace:*",
    "@agent-artifacts/db": "workspace:*",
    "@agent-artifacts/mcp": "workspace:*",
    "@agent-artifacts/shared": "workspace:*",
    "@agent-artifacts/storage": "workspace:*",
    "@hono/node-server": "^2.0.2",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.18",
    "pg-boss": "^12.18.2",
    "zod": "^4.4.3"
  }
}
