{
  "name": "@agent-artifacts/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "artifacts": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "bun run src/cli.ts",
    "typecheck": "tsc -b --pretty false",
    "lint": "tsc -b --pretty false",
    "test": "bun x vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@agent-artifacts/artifact": "workspace:*",
    "commander": "^14.0.3",
    "zod": "^4.4.3"
  }
}
