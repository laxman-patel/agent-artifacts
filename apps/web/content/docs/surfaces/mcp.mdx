---
title: MCP
description: Let compatible agents create, update, and inspect artifacts through tools.
---

## Endpoint

<Callout type="info" title="Agent-native integration">
  MCP is the direct tool surface for agents that can authenticate, discover
  capabilities, and call typed tools without shelling out to the CLI.
</Callout>

The MCP server is exposed at:

```txt
/mcp
```

In local development, clients usually connect through the web origin:

```txt
http://localhost:3000/mcp
```

Next.js rewrites that request to the API service.

## OAuth Discovery

MCP clients can discover authorization metadata from:

```txt
/.well-known/oauth-protected-resource
/.well-known/oauth-authorization-server
```

Standard clients should dynamically register, complete browser consent, exchange the authorization code for a token, then call tools.

## Available Tool Areas

<Cards>
  <Card title="Principal" description="Read the authenticated principal for the current request." />
  <Card title="Projects" description="Create projects, list projects, and check project slug availability." />
  <Card title="Artifacts" description="Create, update, restore, resolve, and check artifact slug availability." />
  <Card title="Share links" description="Create, list, and revoke scoped artifact links." />
  <Card title="Audit" description="List artifact or account audit events." />
  <Card title="Workspaces" description="List workspaces and workspace-visible artifacts." />
</Cards>

## Authorization

MCP calls use the same principal and policy checks as web, CLI, and REST. A tool cannot bypass artifact permissions just because it is called by an agent.
