---
title: REST API
description: The HTTP surface behind the web app, CLI, and custom integrations.
---

## Base Paths

<Callout title="Web-origin friendly by default">
  The API exposes application routes under `/api/*` and health checks at
  `/health`. The web app rewrites `/api/*` to the internal API in local and
  deployed environments so browser cookies stay scoped to the web origin.
</Callout>

## Authentication

Browser sessions use Better Auth routes under `/api/auth/*`. Automation can use bearer credentials, which also skip CSRF checks intended for browser form submissions.

## Core Resources

<Cards>
  <Card title="Profile" description="Return the current principal and visible personal resources." />
  <Card title="Workspaces" description="Manage team membership, invitations, and shared contexts." />
  <Card title="Projects" description="Create and resolve stable project namespaces." />
  <Card title="Artifacts" description="Create, read content, append versions, restore, and manage access." />
  <Card title="Share links" description="Create, list, and revoke scoped viewer or editor links." />
  <Card title="Audit and billing" description="Inspect activity, subscriptions, and storage metering." />
</Cards>

## Path Resolution

Path routes resolve readable owner/project/artifact slugs into canonical resources. Use them when a human-facing URL is the input and an integration needs the underlying object.

## Error Shape

The CLI normalizes API failures into its output envelope, but direct API clients should still treat authentication, authorization, validation, conflicts, and network failures as distinct cases.
