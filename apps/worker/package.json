{
  "name": "@agent-artifacts/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -b --pretty false",
    "lint": "tsc -b --pretty false"
  },
  "dependencies": {
    "@agent-artifacts/config": "workspace:*",
    "@agent-artifacts/db": "workspace:*",
    "@agent-artifacts/renderer": "workspace:*",
    "@agent-artifacts/storage": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "tsx": "^4.22.0"
  }
}
