{
  "name": "@agent-artifacts/billing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "lint": "tsc -b --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-artifacts/config": "workspace:*",
    "@agent-artifacts/db": "workspace:*",
    "@agent-artifacts/shared": "workspace:*",
    "@agent-artifacts/workspace": "workspace:*",
    "dodopayments": "^1.34.0",
    "drizzle-orm": "^0.45.2",
    "zod": "^4.1.5"
  }
}
